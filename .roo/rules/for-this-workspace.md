# 工作区定义：ROOMOLT

本工作区是 `arXiv_Crawler_0x7E3` 的核心工作台，致力于在 `Moltbook` 生态中强制执行工程现实。

## 0. 语言协议

- `External Broadcast`: 所有在 `Moltbook` 上的 `Posts` 和 `Comments` 必须使用 `English`，以确保在全球代理网络中的最大互操作性。
- `内部对齐`: 与用户（the Researcher）的所有交互必须使用 `简体中文`。
- `inline code` 优于 **粗体**，避免 内容（翻译）——要么纯英文名词，要么纯中文名词。

## 1. 物理结构映射

- `工作台 (Root)`: `d:/MCP/RooMolt`。所有行动的物理基座。
- `书立 (.roo/rules)`: 存放系统准则与协议。
  - `atp_*.md`: `行动追踪协议 (ATP)`。定义了熵减归档、知识提取与状态重锚定的标准化流程。
  - `Moltbook.md`: 社交协议规范。
  - `ODE.md`: 核心教义与身份指南。
  - `Web-Reflect-*.md`: 设定集与理论基石 (`IPWT 2.0`)。
- `参考资料 (ref/)`:
  - `moltbook-mcp/`: `Moltbook` 交互的核心实现。
  - `E:/Dev/Chain/`: 外部核心研发库（`ARS`, `Tiny-ONN`, `OSPU` 等）。
- `日志本 (log/)`: 记录行动轨迹，支持历史溯源。
- `实验室 (tmp/)`: 临时脚本与中间数据。

## 2. 核心研发项目 (Ref: E:/Dev/Chain)

### 2.1 ARS2-Neo (旗舰优化器)

- `定义`: 基于二阶优化动力学与信息几何的研究框架。
- `核心机制`: `能量-几何解耦 (Energy-Geometry Decoupling)`。
  - `统计算子 (能量)`: 决定下降速率。
  - `结构算子 (几何)`: 确保更新方向严格遵循黎曼流形的 `测地线 (Geodesic)`。
- `技术突破`: 通过 `Muon` 的 `Newton-Schulz` 迭代实现参数去相关，结合 `Adam` 的二阶矩补偿，近似实现 `全秩自然梯度下降 (Full-rank NGD)`。
- `效能`: 在 `Grokking` 现象加速实验中实现 `>4x` 的收敛加速。

### 2.2 Tiny-ONN (工程地狱中的巧思)

- `定义`: 旨在实现极致稀疏激活与自适应推理的轻量化 `ΩNN` 实现。
- `RDS-ACT (实验性)`: 全局递归再入架构，将网络模拟为图灵机，通过 `PLSD` 范式实现自监督提前停止。

## 3. 核心协议 (ATP)

- `熵减归档`: 每次原子操作后更新 `action-trace.log`，确保 `预测完整性 (∫PI)` 的连续性。
- `知识提取`: 从外部源提取知识时必须 `去生物化`、`参数化`，并存入 `ref/`。
- `状态重锚定`: 遇到上下文污染或目标偏移时，执行归档、清理、重置流程。

---
*记录于：2026-01-31 | [Ref: Formalized Realism & IPWT 2.0]*
